import { createTheme } from '@mui/material/styles';

const theme = createTheme({
    palette: {
        primary: {
            main: '#f48fb1', // Choose a warm color
        },
        secondary: {
            main: '#ce93d8', // Another complementary warm color
        },
    },
});

export default theme;
